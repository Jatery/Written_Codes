#include <stdio.h>
#include <assert.h>

#define DEBUG

#define f(k) (77 * k + 2222) % s
#define maxn 105
#define maxs maxn * maxn
#define maxc 10
#define maxk 2e7

int main() {
	int n, s, c;
	scanf("%d%d%d", &n, &s, &c);
	
	assert(n <= maxn);

	int tower[n][n][n];

	
}
